package jp.speakbuddy.edisonandroidexercise.base

import androidx.activity.ComponentActivity

open class BaseActivity : ComponentActivity() {
    /**
    Usually there is always a reason to have a Base/Core Activity that all activities inherit from
    I couldn't find anything to add into this one, but adding it anyway since I did the same for VM
     **/
}